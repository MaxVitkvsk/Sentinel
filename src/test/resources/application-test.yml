app:
  scheduling:
    enabled: false
  kafka:
    partitions: 3
    replicas: 3

coingecko:
  api:
    url: http://localhost:${wiremock.server.port}
    key: test-key

wiremock:
  server:
    port: 8080